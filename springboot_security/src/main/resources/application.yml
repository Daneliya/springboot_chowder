server:
  port: 9001
spring:
  application:
    name: springboot-security #指定服务名
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/test?characterEncoding=utf-8&serverTimezone=UTC
    username: root
    password: xxl666
  redis:
    host: localhost
    port: 6379
    password:
filter:
  path:
    includes: /user/login,/user/logout
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml